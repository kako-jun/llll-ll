{
  "name": "llll-ll",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "zola:build": "cd zola && node scripts/sync-nostalgic-bbs.mjs && zola build",
    "zola:sync-comments": "cd zola && node scripts/sync-nostalgic-bbs.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.6",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.6"
  }
}
